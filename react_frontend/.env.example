# Backend API Configuration (Vite)
# Copy this to `.env.local` and edit values as needed for your environment
# Note: Vite exposes only variables prefixed with `VITE_` to the client-side code
VITE_API_URL=http://127.0.0.1:8000  # Update this URL for production
VITE_API_BASE_PATH=/api